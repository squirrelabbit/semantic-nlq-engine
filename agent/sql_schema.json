{
  "type": "object",
  "required": ["sql"],
  "additionalProperties": false,
  "properties": {
    "sql": {
      "type": "string",
      "description": "Read-only SELECT SQL using available tables/columns"
    },
    "notes": {
      "type": "string"
    }
  }
}
