{
  "type": "object",
  "required": ["original_question", "datasets"],
  "additionalProperties": false,
  "properties": {
    "original_question": {
      "type": "string",
      "description": "The original user question, unchanged"
    },
    "datasets": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Candidate dataset table names (2-3)."
    },
    "confidence": {
      "type": "number",
      "minimum": 0,
      "maximum": 1
    },
    "notes": {
      "type": "string"
    }
  }
}
